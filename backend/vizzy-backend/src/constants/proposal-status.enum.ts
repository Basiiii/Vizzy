/**
 * Enum representing the possible states of a proposal
 * @enum {string}
 */
export enum ProposalStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  CANCELLED = 'cancelled',
}
